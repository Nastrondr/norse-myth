# 耶梦加得（Jormungandr）高危交互系统

## 一、定位概述

耶梦加得是北欧神话中的世界之蛇，环绕米德加德边界的灾厄生物。作为高危观测对象，它不支持普通的羁绊养成系统（不显示羁绊、徽章、投喂、合作等模块），仅提供受限的观测互动功能。

**核心定位**：只能被观测、记录、远离，可能反向影响用户状态。

---

## 二、状态系统

### 2.1 状态字段

| 字段名 | 默认值 | 说明 | 最大值 |
|--------|--------|------|--------|
| observation | 0 | 观测深度，记录观测进度 | 40 |
| sanityLoad | 0 | 心智负荷，精神状态指标 | - |
| hostility | 0 | 居民敌视，村民态度指标 | - |
| contactStatus | 高危观测中 | 当前接触状态 | - |
| recentActions | [] | 最近行为记录 | - |
| frozen | false | 冻结状态，是否禁用互动 | - |
| unlockedEvents | [] | 已解锁的事件 | - |

### 2.2 心智负荷等级

| 数值范围 | 状态描述 |
|----------|----------|
| 0 - 3 | 清醒 |
| 4 - 6 | 不安 |
| 7 - 9 | 执迷 |
| 10+ | 精神濒临崩溃 |

### 2.3 居民敌视等级

| 数值范围 | 状态描述 |
|----------|----------|
| 0 - 2 | 无人注意 |
| 3 - 5 | 有人议论 |
| 6 - 8 | 居民敌视 |
| 9+ | 被驱逐边缘 |

### 2.4 观测深度显示

显示为百分比，计算公式：
```
Math.min(100, observation * 5)
```

---

## 三、互动按钮系统

### 3.1 按钮解锁条件

| 按钮 | 解锁条件 | 风险等级 | 按钮样式 |
|------|----------|----------|----------|
| 远观 | 初始解锁 | 低 | 普通样式 |
| 听潮 | 初始解锁 | 中低 | 普通样式 |
| 观察 | 观测深度 ≥ 5 | 高 | 红色边框 |
| 退后 | 观测深度 ≥ 5 | 安全 | 绿色边框 |
| 祭拜 | 观测深度 ≥ 10 | 高 | 红色边框 |
| 触碰 | 观测深度 ≥ 20 | 极高 | 红色强边框 |

### 3.2 按钮代码实现

```javascript
jormungandrActions() {
    const obs = Number(this.jormungandrState.observation || 0)
    const actions = [
        { key: 'distantView', label: '远观', risk: 'low' },
        { key: 'tideListen', label: '听潮', risk: 'medium' }
    ]
    if (obs >= 5) {
        actions.push({ key: 'observe', label: '观察', risk: 'high' })
    }
    if (obs >= 5) {
        actions.push({ key: 'retreat', label: '退后', risk: 'safe' })
    }
    if (obs >= 10) {
        actions.push({ key: 'worship', label: '祭拜', risk: 'high' })
    }
    if (obs >= 20) {
        actions.push({ key: 'touch', label: '触碰', risk: 'extreme' })
    }
    return actions
}
```

---

## 四、行为效果

### 4.1 行为效果表

| 行为 | 观测深度 | 心智负荷 | 居民敌视 | 反馈消息 |
|------|----------|----------|----------|----------|
| 远观 | +1 | 0 | 0 | 你站在安全距离外观察。 |
| 听潮 | +1 | +1 | 0 | 潮声中似乎夹杂着某种节律。 |
| 观察 | +2 | +2 | 0 | 你的笔记越来越密。 |
| 祭拜 | +1 | +2 | +3 | 远处，有人在看你。 |
| 退后 | 0 | -2 | -1 | 你选择后退，危险有所降低。 |
| 触碰 | +3 | +4 | +2 | 你越过了安全距离。海面忽然安静得可怕。 |

### 4.2 触碰确认弹窗

触碰按钮需要用户确认，弹窗内容：

```
title: 确认
content: 你确定要靠近海面吗？有些边界一旦越过，就不再由你决定返回。
confirmText: 靠近
cancelText: 后退
```

用户选择后退时：
```
this.addJormungandrMessage('你选择了后退。')
```

### 4.3 行为代码实现

```javascript
executeJormungandrAction(actionKey) {
    const state = this.jormungandrState

    state.recentActions.push(actionKey)
    if (state.recentActions.length > 6) {
        state.recentActions.shift()
    }

    switch (actionKey) {
        case 'distantView':
            state.observation += 1
            this.addJormungandrMessage('你站在安全距离外观察。')
            break
        case 'tideListen':
            state.observation += 1
            state.sanityLoad += 1
            this.addJormungandrMessage('潮声中似乎夹杂着某种节律。')
            break
        case 'observe':
            state.observation += 2
            state.sanityLoad += 2
            this.addJormungandrMessage('你的笔记越来越密。')
            break
        case 'worship':
            state.observation += 1
            state.sanityLoad += 2
            state.hostility += 3
            this.addJormungandrMessage('远处，有人在看你。')
            break
        case 'retreat':
            state.sanityLoad = Math.max(0, state.sanityLoad - 2)
            state.hostility = Math.max(0, state.hostility - 1)
            this.addJormungandrMessage('你选择后退，危险有所降低。')
            break
        case 'touch':
            state.observation += 3
            state.sanityLoad += 4
            state.hostility += 2
            this.addJormungandrMessage('你越过了安全距离。海面忽然安静得可怕。')
            break
    }

    this.playAbyssAnimation = true
    setTimeout(() => {
        this.playAbyssAnimation = false
    }, 500)

    this.checkJormungandrEvents()
    this.saveJormungandrState()
}
```

---

## 五、事件触发机制

### 5.1 recentActions 机制

每次行为后，将 action 加入 `recentActions`：
```javascript
state.recentActions.push(actionKey)
```

只保留最近 6 次：
```javascript
if (state.recentActions.length > 6) {
    state.recentActions.shift()
}
```

### 5.2 事件一：海边的低语者

**触发条件**（二选一）：
- 最近 5 次行为中「祭拜」≥ 3 次
- 居民敌视 ≥ 8

**事件描述**：
> 你开始频繁地向海献上牲畜、盐与黑色绳结。起初只是几只羊，后来连村里的人也注意到了异常。他们看见你在潮水边跪下，口中喃喃念着听不懂的词。有人说你在祈求保护，也有人说你在喂养某个不该被喂养的东西。从那天起，居民不再愿意靠近你。

**后果**：
- `hostility += 2`
- `contactStatus = '被居民敌视'`
- 记录到 `unlockedEvents`

### 5.3 事件二：最后一页潮汐记录

**触发条件**（同时满足）：
- 最近 6 次行为中「观察」≥ 4 次
- 心智负荷 ≥ 9

**事件描述**：
> 你的记录越来越密。潮汐、梦境、鱼群死亡的方向、夜里传来的低鸣……每一项都被你写进档案。起初，你还能分辨哪些是观察，哪些是幻觉。后来，纸上的线条开始像鳞片，海图上的弧线开始像一段身体。某天清晨，你没有再回到家人与同伴身边。人们只在海岸边找到被盐水浸透的笔记。

**后果**：
- `contactStatus = '失踪'`
- `frozen = true`
- 所有互动按钮禁用
- 记录到 `unlockedEvents`

**注意**：这是神话化失踪结局，不是死亡结局，不涉及奖励徽章。

### 5.4 危险警告

**触发条件**：
- 心智负荷 ≥ 7 且 < 10
- 未触发过警告

**警告消息**：
> 你应该离开。

**特点**：
- 仅触发一次，不会重复显示
- 消息显示为暗红色警告风格

### 5.5 事件检测代码实现

```javascript
checkJormungandrEvents() {
    const state = this.jormungandrState
    if (state.frozen) return

    const recent5Actions = state.recentActions.slice(-5)
    const recent6Actions = state.recentActions.slice(-6)
    const worshipCount = recent5Actions.filter(a => a === 'worship').length
    const observeCount = recent6Actions.filter(a => a === 'observe').length

    // 事件二：最后一页潮汐记录（优先检查）
    if (!state.unlockedEvents.includes('tide-record') && observeCount >= 4 && state.sanityLoad >= 9) {
        state.unlockedEvents.push('tide-record')
        state.contactStatus = '失踪'
        state.frozen = true
        this.addJormungandrMessage('你的记录越来越密...')
        return
    }

    // 事件一：海边的低语者
    if (!state.unlockedEvents.includes('whisperer') && (worshipCount >= 3 || state.hostility >= 8)) {
        state.unlockedEvents.push('whisperer')
        state.hostility += 2
        state.contactStatus = '被居民敌视'
        this.addJormungandrMessage('你开始频繁地向海献上牲畜...')
    }

    // 危险警告
    if (state.sanityLoad >= 7 && state.sanityLoad < 10 && !state.unlockedEvents.includes('warning')) {
        state.unlockedEvents.push('warning')
        this.addJormungandrMessage('你应该离开。')
    }
}
```

### 5.6 事件触发条件对照表

| 事件 | 触发条件 | 优先级 |
|------|----------|--------|
| 最后一页潮汐记录 | 最近6次「观察」≥4 且 心智负荷≥9 | 高（优先检查） |
| 海边的低语者 | 最近5次「祭拜」≥3 或 居民敌视≥8 | 中 |
| 危险警告 | 心智负荷≥7 且<10 | 低 |

---

## 六、UI显示模块

### 6.1 档案卡片

**显示内容**：
- 接触状态（高危观测中 / 被居民敌视 / 失踪）
- 观测深度（进度条 + 百分比）
- 心智负荷（进度条 + 数值 + 状态描述）

### 6.2 互动按钮区域

- 根据观测深度动态显示可用按钮
- 危险等级用边框颜色区分
- 点击播放粒子动画

### 6.3 事件日志

**显示最近 3 条互动消息**

**样式规则**：
- 普通消息：灰色背景，灰色文字
- "你应该离开。"：暗红色警告风格（红色背景、红色文字）

### 6.4 事件记录卡片

触发事件后显示事件详情：
- 事件名称（金色标题）
- 事件描述（灰色正文）

### 6.5 冻结状态

当 `frozen = true` 时：
- 显示"档案冻结"标题
- 显示"该档案已进入失踪状态。后续互动已冻结。"
- 互动按钮全部禁用

---

## 七、本地存储

### 7.1 存储 Key

```
jormungandr_high_risk_state
```

### 7.2 存储结构

```json
{
  "observation": 0,
  "sanityLoad": 0,
  "hostility": 0,
  "contactStatus": "高危观测中",
  "recentActions": [],
  "frozen": false,
  "unlockedEvents": []
}
```

### 7.3 存储时机

1. **读取**：页面加载时（`onLoad`）
2. **保存**：每次行为后（`saveJormungandrState`）

### 7.4 加载逻辑

```javascript
loadJormungandrState() {
    const saved = uni.getStorageSync('jormungandr_high_risk_state')
    if (saved) {
        this.jormungandrState = {
            observation: 0,
            sanityLoad: 0,
            hostility: 0,
            contactStatus: '高危观测中',
            recentActions: [],
            frozen: false,
            unlockedEvents: [],
            ...saved
        }
    }
}
```

---

## 八、重置机制

### 8.1 移除记录重置

点击「移除记录」按钮时，重置所有状态：

```javascript
resetJormungandrState() {
    this.jormungandrState = {
        observation: 0,
        sanityLoad: 0,
        hostility: 0,
        contactStatus: '高危观测中',
        recentActions: [],
        frozen: false,
        unlockedEvents: []
    }
    this.jormungandrMessages = []
    uni.removeStorageSync('jormungandr_high_risk_state')
}
```

**影响**：
- 所有状态值重置为默认值
- 事件日志清空
- 本地存储删除
- 重新进入时从初始状态开始

---

## 九、交互流程图

```
开始
  ↓
进入图鉴详情页
  ↓
加载数据 → 是耶梦加得？
  ↓
显示高危观测档案
  ↓
用户选择行为
  ↓
检查冻结状态 ──冻结──→ 显示"档案已冻结" → 结束
  ↓ (未冻结)
执行行为 → 更新状态值
  ↓
检查事件触发
  ↓
触发事件？ → 是 → 更新状态/冻结
  ↓ (否)
显示警告？
  ↓ (是)
显示"你应该离开。"
  ↓
保存状态到本地
  ↓
结束
```

---

## 十、数据配置

### 10.1 norse.js 数据配置

```javascript
{
    id: 'jormungandr',
    name: '耶梦加得',
    originalName: 'Jörmungandr',
    type: '巨蛇',
    realm: 'midgard',
    dangerLevel: '极危',
    interactionType: 'abyss',
    description: '洛基与安格尔波达之子，被奥丁投入大海，最终长成环绕人间世界的巨蛇。',
    power: '毒液环绕世界',
    relatedStories: ['ragnarok'],
    collected: true,
    status: 'observed',
    abyssAwareness: 0,
    abyssMax: 100,
    riskLevel: 0,
    riskMax: 100,
    abyssFrozen: false,
    missing: false,
    lastSignal: '无',
    interactionCount: 0,
    highRiskCount: 0,
    likes: ['深海', '边界', '潮汐'],
    dislikes: ['雷霆', '挑衅', '靠近'],
    interactionStats: {
        distantView: 0,
        tideListen: 0,
        worship: 0,
        record: 0,
        retreat: 0,
        touch: 0
    }
}
```

---

## 十一、样式规范

### 11.1 颜色系统

| 元素 | 颜色值 |
|------|--------|
| 背景 | #0B1118 |
| 卡片背景 | #172230 |
| 边框 | #27384A |
| 主文字 | #F2F4F6 |
| 次文字 | #A8B3BD |
| 暗金 | #C6A15B |
| 危险红 | #B94A48 |
| 冷灰 | #66727F |

### 11.2 警告样式

| 元素 | 样式 |
|------|------|
| 背景 | rgba(185, 74, 72, 0.15) |
| 边框 | rgba(185, 74, 72, 0.3) |
| 时间文字 | #B94A48 |
| 内容文字 | #D47573 |

### 11.3 按钮风险等级样式

| 风险等级 | 边框样式 |
|----------|----------|
| 低 | 普通灰色边框 |
| 中低 | 黄色边框 |
| 高 | 红色边框 |
| 极高 | 红色强边框 |
| 安全 | 绿色边框 |

---

## 十二、代码位置索引

| 功能模块 | 文件路径 | 行号范围 |
|----------|----------|----------|
| 状态初始化 | creature-detail.vue | 416-424 |
| 计算属性 | creature-detail.vue | 447-486 |
| 行为处理 | creature-detail.vue | 1196-1270 |
| 事件检测 | creature-detail.vue | 1271-1299 |
| 消息添加 | creature-detail.vue | 1300-1307 |
| 事件信息 | creature-detail.vue | 1309-1325 |
| 本地存储 | creature-detail.vue | 1178-1195 |
| 重置状态 | creature-detail.vue | 720-735 |
| 数据配置 | norse.js | 459-490 |

---

## 十三、验收标准

| 序号 | 验收项 | 预期结果 |
|------|--------|----------|
| 1 | 收藏后图鉴卡片显示 | 正常显示名称和图标，不显示"???" |
| 2 | 隐藏普通模块 | 不显示羁绊、徽章、投喂、合作模块 |
| 3 | 显示高危观测档案 | 显示观测深度、心智负荷 |
| 4 | 按钮逐级解锁 | 按观测深度解锁按钮 |
| 5 | 祭拜事件触发 | 祭拜≥3次触发"海边的低语者" |
| 6 | 观察事件触发 | 观察≥4次且心智≥9触发"最后一页潮汐记录" |
| 7 | 失踪后冻结 | 触发事件后禁用所有互动按钮 |
| 8 | 本地存储 | 刷新页面状态保留 |
| 9 | 移除记录重置 | 点击移除记录后所有状态清空 |
| 10 | 普通生物不受影响 | 其他深渊生物保持原有逻辑 |

---

## 十四、版本历史

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-05-19 | 初始版本，完整实现高危交互系统 |

---

**文档创建日期**：2026-05-19
**最后更新**：2026-05-19
